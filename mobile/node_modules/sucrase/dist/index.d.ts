import CJSImportProcessor from "sucrase/dist/CJSImportProcessor";
import { RawSourceMap } from "sucrase/dist/computeSourceMap";
import { HelperManager } from "sucrase/dist/HelperManager";
import NameManager from "sucrase/dist/NameManager";
import type { Scope } from "sucrase/dist/parser/tokenizer/state";
import TokenProcessor from "sucrase/dist/TokenProcessor";
export interface TransformResult {
    code: string;
    sourceMap?: RawSourceMap;
}
export interface SucraseContext {
    tokenProcessor: TokenProcessor;
    scopes: Array<Scope>;
    nameManager: NameManager;
    importProcessor: CJSImportProcessor | null;
    helperManager: HelperManager;
}
export declare type Options = import("sucrase/dist/Options").Options;
export declare type SourceMapOptions = import("sucrase/dist/Options").SourceMapOptions;
export declare type Transform = import("sucrase/dist/Options").Transform;
export declare function getVersion(): string;
export declare function transform(code: string, options: Options): TransformResult;
/**
 * Return a string representation of the sucrase tokens, mostly useful for
 * diagnostic purposes.
 */
export declare function getFormattedTokens(code: string, options: Options): string;
